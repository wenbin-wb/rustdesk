.hvigor/
.preview/
build/
# Created by the native build tasks; appears as soon as cpp/ exists under src/main.
.cxx/
oh_modules/
*.hap
*.hsp
*.har
local.properties
node_modules/
# Native libraries are built locally by build_bridge.ps1 and are not source, the same way
# the Android build treats jniLibs. Run that script after cloning before building the HAP.
# The module-root libs/ is where hvigor picks prebuilt .so files up from.
entry/libs/
!**/resources/**/media/*.png
!**/resources/**/media/*.svg
